<h2>Check out our properties!</h2>
<ul>
  <% @properties.each do |property| %>
    <li><%= property.title %></li>
  <% end %>
</ul>
 <% @properties.each do |property| %>
<div>
    <li><%= image_tag property.image_url %></li>
</div>
  <% end %>
